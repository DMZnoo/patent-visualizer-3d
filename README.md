# Heliase POC testing tool

To install:
`yarn install`

To start the server:
` yarn dev `

## Acceptable Data Type
Please refer to global.d.types.ts file for the detail.
Note that `node group` parameter expects `positions` array which will be [[group_1 position], [group_2 position]].

